---
import { type CollectionEntry, render } from 'astro:content'

interface Props {
  entry: CollectionEntry<'projectsContent'>
}

const { entry } = Astro.props
const { Content } = await render(entry)
---

<section
  class="bg-linear-to-t from-popover to-transparent bg-size-[auto_24rem] bg-bottom bg-no-repeat py-8 pb-24 md:py-10 md:pb-28"
>
  <div class="wrapper">
    <div class="mx-auto prose">
      <Content />
    </div>
  </div>
</section>
