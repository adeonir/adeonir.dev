---
import { getEntry } from 'astro:content'

import Logo from '~/components/logo.astro'

const footer = await getEntry('footer', 'footer')

if (!footer) {
  throw new Error('Missing footer/footer entry')
}

const { brand, tagline } = footer.data
---

<footer class="border-t border-border bg-sunken">
  <div
    class="wrapper flex flex-col gap-6 py-12 md:flex-row md:items-center md:justify-between"
  >
    <div class="flex flex-col gap-2">
      <a
        href="/"
        class="-mx-1.5 -my-0.5 shrink-0 self-start rounded-sm px-1.5 py-0.5 transition focus-visible:ring-4 focus-visible:ring-ring/70 focus-visible:ring-offset-2 focus-visible:ring-offset-background focus-visible:outline-none"
      >
        <Logo class="w-28" label={brand.text} />
      </a>
      <p class="font-mono text-caption text-muted-foreground">
        {brand.copyright}
      </p>
    </div>
    <p class="text-code text-muted-foreground">
      {
        tagline.map((segment) =>
          segment.emphasis ? (
            <span class="text-emphasis">{segment.text}</span>
          ) : (
            segment.text
          ),
        )
      }
    </p>
  </div>
</footer>
