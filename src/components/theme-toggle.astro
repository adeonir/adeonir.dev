---
import { ThemeToggle as ThemeToggleIsland } from '~/components/islands/theme-toggle'
import { parseLocale } from '~/helpers/content'
import { getLocalizedEntry } from '~/services/localized'

const locale = parseLocale(Astro.currentLocale)
const labels = await getLocalizedEntry('themeToggle', locale)
---

<ThemeToggleIsland client:load labels={labels.data} />
